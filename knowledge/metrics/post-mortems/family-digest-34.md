# POST-MORTEM: family-digest PR #34 — Add Claude-generated weekly recap to Friday digest

Branch: `feat/week-recap` → `main` | Author: padminipyapali | 30 min wall time
Size: +1231 −7 across 20 files, 1 commit

## Context

Adds a 4-paragraph narrative recap section ("This week, looking back") to the top of the Friday digest, generated by Claude (`claude-sonnet-4-6`) from the past Sat → Fri calendar window. First LLM integration in the repo. Opt-in via `ANTHROPIC_API_KEY`; silently omitted when unset.

## Local Review (pre-push)

Adversarial review ran via project hook (PASS, no FAIL findings). Fresh-context critic agent reviewed before commit and surfaced 8 observations — 0 FAIL, 2 WARN (duplicated zoned-time helpers between `pipeline.ts` and `recap/compose.ts`; minor test gaps). One test gap addressed before commit (adversarial HTML XSS-shape test in `html.test.ts`). Other WARN items deferred to follow-up cleanup. CodeRabbit not invoked.

## Step Compliance

Not recorded in PR body. Actual flow: plan agent → implementer agent → critic agent (fresh context) → critic-fix loop (one test added) → commit → adversarial review (hook-gated) → push → PR → self-merge.

## Review Friction (post-push)

0 reviews, 0 inline comments, 0 general comments. Self-merged 30 min after creation. Solo operator; no peer review.

## Adversarial Review Effectiveness

PASS. The Tier 0 grep + LLM-integration checklist sections caught nothing because the implementer's design already followed every relevant rule (constant system prompt, JSON-framed user message, escape on render). Critic catch was the higher-value gate; adversarial review confirmed the patterns without finding new issues.

## Fix-up Metrics

- **Post-merge fix rate:** 0% (0 follow-up commits within 48h).
- **Pre-merge catch rate by step:**
  - `4b` (internal critic review): 1 fix cycle (one adversarial test added based on critic feedback).
- **Pre-merge iteration count:** 1 (healthy).
- **Fix-up taxonomy:** 1 `test-quality` (adversarial XSS coverage).

## Planning Quality

- Description: **complete** (Summary, Sample output, Security, Post-merge ops, Follow-ups, Test plan).
- Scope: **clean** — single concern (new recap section), single commit, +1231 LOC contained within `src/recap/` plus minimal touch-points in 6 existing files for wiring.
- Branch lifetime: ~30 minutes (worktree → merge).
- Performance/cost section: **present** (≈ $0.005/run, ≈ $0.25/year, latency overlapped via `Promise.all` with weather fetch). Satisfies project Planning Requirement.
- Entry-point enumeration: Both `scripts/send.ts` and `scripts/digest.ts` (`buildProdDeps`) wired; both `draft` and `send` CLI subcommands carry the client. Both `family-digest` and `digest-cron` Railway services share the image — only `digest-cron` needs the API key set (documented in PR body).

## Code Quality Signals

- No recurring patterns (single PR).
- Two follow-up patterns surfaced by critic, captured as cleanup items in PR body — not regressions:
  1. Duplicated zoned-time helpers (`partsInZone`, `zonedMidnightUtc`, `addDaysZoned`) across `pipeline.ts` and `recap/compose.ts`. Project rule "extract shared utility when logic appears in 2+ components" applies; queued as follow-up.
  2. Test gaps for cross-year window, fence-only stripping, three-asterisk markdown edge. Lower priority — happy path + failure modes already covered.

## Process Efficiency

- **Iteration:** efficient (1 round end-to-end).
- **Automation potential:** the only fix between critic and ship was the adversarial HTML XSS test. This is exactly the kind of test the adversarial-review checklist could auto-generate when an LLM is in the pipeline. Consider adding an LLM-output rendering check as a Tier 1 item in `adversarial-review.md` for any PR touching files that render LLM-derived content.
- **PR-body template gap:** Neither family-digest PR (#33, #34) included `## Local Review` or `Steps skipped:` sections, leaving step compliance untrackable. Worth adopting the body template used in other projects.

## Knowledge Updates

Adding the LLM-rendering pattern to `~/.claude/knowledge/llm-integration.md`. Confirmed existing entries (constant system prompt, JSON-framed user data, code-fence stripping) hold up under adversarial review — strengthening rather than replacing.

## Recommendations

1. **Add an LLM-renderer adversarial check.** When a PR introduces a code path that renders LLM-generated text into HTML, the checklist should mandate an explicit XSS-shape test. This PR caught it via critic, but it's a class of issue worth automating.
2. **Adopt PR body template.** A short `## Local Review` and `Steps skipped:` section unlocks step-compliance trending across PRs. Two consecutive family-digest PRs have left this empty.
3. **Schedule the zoned-time extraction follow-up.** Duplicated date math between `pipeline.ts` and `recap/compose.ts` will rot if not extracted soon. Small, contained PR; ideal candidate for the next session.
4. **Verify Friday's send.** First production run with the recap is 2026-05-15 23:00 UTC. Inspect the Gmail output Saturday morning to confirm: (a) recap section renders correctly, (b) digest still sends if the API key is removed (failure-mode validation).
