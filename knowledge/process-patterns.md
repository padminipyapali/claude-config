# Process Patterns

Cross-project learnings about the development process: review efficiency,
planning discipline, iteration velocity, and automation opportunities.

## Review Efficiency

- **Bot-only review catches real bugs but inflates round count.** CodeRabbit found 4 correctness issues on first review of PR #131, producing 2 review rounds and 75% fix-up ratio. All fixes were mechanical (<20 min total), but the commit noise adds up. Expect 2+ rounds as baseline when only bot reviews. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **Self-merge with bot-only review misses architectural feedback.** Bot reviewers catch syntax and correctness but can't assess "is this the right abstraction?" or "does this belong in this file?" No human review means architecture debt accumulates silently. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->

## Planning Discipline

- **Thorough planning reduces implementation rework.** PR #131 had a detailed plan (entry points, DRY analysis, adversarial review section, performance/cost) and zero redesign commits. All fix commits were review-driven, not planning gaps. The plan paid off. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->

## Follow-Up Discipline

- **Dedicated follow-up PRs for post-push findings close the loop fastest.** folio PR #2 (3 LOC, 9 min to merge) addressed a specific post-push finding from PR #1 (inline DATE parsing in TrendChart). The fix was scoped to exactly one concern, verified complete via codebase grep, and merged with 0% fix-up ratio. This is the ideal pattern: post-push findings become their own focused PR rather than being deferred to a tracker or bundled into the next feature. The tight feedback loop (PR #1 merged -> PR #2 fixes finding within hours) prevents the finding from going stale. <!-- Source: post-mortem, folio #2, 2026-02-23 -->

- **Post-mortem infrastructure gaps should be addressed in dedicated tooling PRs.** folio PR #1 post-mortem noted "step 4e (lint) skipped because ESLint not configured." PR #3 directly closed that gap with a dedicated ESLint config PR. The turnaround was same-day (PR #1 merged, PR #3 created hours later). This is the ideal pattern for infrastructure debt: post-mortem identifies gap -> dedicated PR fixes it -> future PRs benefit from the tooling. Compare with feature PRs that bundle tooling fixes, which conflate concerns and make review harder. <!-- Source: post-mortem, folio #3, 2026-02-23 -->

## Scope Decisions

- **Deferring trivial interface extractions creates fix commits.** command-center PR #21 deferred `IVelocityService` interface extraction (~3 lines) as "scope creep" during local review. CodeRabbit then flagged it post-push, requiring a fix commit. For extractions under 10 lines that match an established project convention, include them in the original PR. The "scope creep" label should be reserved for behavioral additions, not pattern compliance. <!-- Source: post-mortem, command-center #21, 2026-02-19 -->
- **Bundling tooling setup with feature PRs inflates LOC and review scope.** PR #228 bundled stylelint config + CSS autofix (commit 2, ~1100 LOC of CSS changes) with a performance optimization (commit 1, ~300 LOC). CodeRabbit's pre-merge check flagged "out-of-scope changes." The tooling work inflated LOC from ~500 to ~1600, pushing it past the 600 LOC threshold and generating style-only review findings that wouldn't exist in a focused PR. When adding a new lint tool, create a separate tooling PR for the config + autofix, then reference it from the feature PR. This is the inverse of PR #213's pattern (scope creep from UI changes) -- here the scope creep is infrastructure. <!-- Source: post-mortem, second-brain #228, 2026-02-25 -->
- **"Keep-in-sync" comments are a code smell -- extract instead.** second-brain PR #198 duplicated `buildTodoInlineButtons` across telegram.ts and todo-list-handler.ts with "keep in sync" comments. The adversarial review identified the duplication but deemed it acceptable. CodeRabbit flagged it post-push, requiring a fix commit that extracted the shared utility (net -9 lines). When identical logic exists in 2+ files and is under ~30 lines, always extract to a shared utility rather than commenting "keep in sync." The comment is a maintenance liability that the extraction eliminates. <!-- Source: post-mortem, second-brain #198, 2026-02-21 -->

## Adversarial Review Gaps

- **JSDoc is the dominant post-push finding category for new component PRs.** nanny-app PR #35 had 4 of 7 post-push findings related to missing JSDoc on exported components/functions. The adversarial checklist Tier 4 "Documentation sync" mentions JSDoc but has no mechanical grep step. A Tier 0 grep for exported functions/components without JSDoc in changed `.tsx` files would catch this class. <!-- Source: post-mortem, nanny-app #35, 2026-02-20 -->
- **Adversarial review can identify but incompletely fix error handling patterns.** nanny-app PR #35's adversarial review listed "async error handling" as a finding and partially addressed it, but the core bug (onComplete() called unconditionally after catch) shipped. The Tier 1.2 mechanical verification ("For each catch, what happens on the happy path after it?") was not fully executed. When adversarial review identifies a class of issue, verify the FIX is complete -- not just that the issue was acknowledged. <!-- Source: post-mortem, nanny-app #35, 2026-02-20 -->
- **JSDoc audit scope: fix the diff, miss the file.** PR #185's adversarial review caught "stale JSDoc" as a pre-push finding and fixed it, but Copilot found a different JSDoc inaccuracy in the same file (`MessageIntent` doc comment listed only `CHAT` but type also includes `TODO_LIST`, `DAILY_SUMMARY`, `MORNING_BRIEF`, `CALENDAR`). When fixing JSDoc as an adversarial finding, audit ALL doc comments in the changed file — not just the one directly affected by the diff. <!-- Source: post-mortem, second-brain #185, 2026-02-20 -->
- **useMemo dependency completeness is a recurring React review gap.** nanny-app PR #28 shipped incomplete useMemo dependencies in both Dashboard and HoursTracker -- memoized getPayPeriod(new Date(), config) without a date-based dependency key, causing stale results after midnight. Local adversarial review missed this in both components. The checklist needs a React-specific check: "For useMemo wrapping Date computations, verify a day-based key (e.g., getTodayISO()) is in the dependency array." <!-- Source: post-mortem, nanny-app #28, 2026-02-20 -->
- **JS Date constructor silent normalization bypasses NaN guards.** nanny-app PR #28's anchor validation checked for NaN but not calendar-invalid dates (e.g., '2026-02-30' -> March 2). new Date(year, month-1, day) silently normalizes, so the malformed fallback never triggers. Fix: after construction, verify getFullYear/getMonth/getDate match input components. Add to Tier 3 defensive coding checks. <!-- Source: post-mortem, nanny-app #28, 2026-02-20 -->
- **Checklist items present but not mechanically executed.** "UTC suffix in test Date strings" was literally in Tier 3 of the adversarial review checklist, yet test dates without Z suffix shipped. The bottleneck is execution discipline, not checklist coverage. Fix: run grep patterns, not just read items. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **Unmount guard pattern known but not applied to new code.** PR #140's adversarial review reported 0 findings, but CodeRabbit caught a missing `isMountedRef` unmount guard — a pattern already documented in `react-patterns.md` and used by 3 sibling hooks in the same file. The adversarial review needs to cross-reference new async hooks against sibling patterns in the same file. <!-- Source: post-mortem, second-brain #140, 2026-02-17 -->
- **Copy-and-adapt defeats adversarial review discipline.** PR #143 adapted the inline-edit pattern from PR #142 to the TODO panel. The adversarial review claimed to find and fix 3 issues (Escape propagation, keyboard a11y, state cleanup), but all 3 shipped incomplete — CodeRabbit caught `role="button"` on span, Escape only on textarea, and missing save-guard. The checklist items (Tier 3: "Semantic elements", "Escape in edit-within-panel") have precise wording that would have prevented these. Root cause: "I already fixed this" cognitive shortcut when adapting patterns. Fix: re-run the checklist against the NEW instance, not assume coverage from the prior fix. <!-- Source: post-mortem, second-brain #143, 2026-02-17 -->
- **New gap: String truncation arithmetic.** When slicing + appending to fit a max length, verify `slice_length + suffix_length <= limit`. Added to knowledge but not yet in adversarial checklist. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **New gap: User-facing text compound wrapping.** When a helper function adds decoration (e.g., parentheses) and callers add more, the result compounds (e.g., `((all day))`). Review all format helper return values against their call sites. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **New gap: Test value swap vs. behavioral assertion.** When updating tests for a behavioral change, the adversarial review should verify whether the test asserts the OLD behavior with a new value or the NEW behavior entirely. PR #145 changed `"Interesting thought!"` to `"Thought saved."` in `toHaveBeenCalledWith` but did not change the assertion to `not.toHaveBeenCalled`, which was the actual behavioral change specified in the requirement. Value swaps are mechanical; behavioral verification requires reading the spec. <!-- Source: post-mortem, second-brain #145, 2026-02-17 -->
- **New gap: Test env variable isolation.** When tests mutate `process.env.*` (set in `beforeEach`, deleted in a test), the adversarial review should verify cleanup in `afterEach`. Without restore, env mutations leak to subsequent test files. The test-only category in the checklist lacks test isolation checks. <!-- Source: post-mortem, second-brain #148, 2026-02-17 -->
- **New gap: Missing error branch test coverage.** When a route has distinct error paths (timeout/AbortError -> 504, non-404 error -> 502), tests should cover each branch. The adversarial review should verify that all catch/error branches in new route handlers have corresponding test cases. <!-- Source: post-mortem, second-brain #148, 2026-02-17 -->
- **Test mock target verification.** When mocking service methods in test files, verify the mock targets the correct method for the code path under test. PR #159 mocked `findTodosForDate` (Due Today section) when the tested behavior used `findAllOpenTodos` (Open section) — a critical-severity finding caught post-push. The adversarial review should trace each mock to the production code path it simulates. <!-- Source: post-mortem, second-brain #159, 2026-02-19 -->
- **New gap: Conditional column selection for type-discriminated queries.** When a SQL query spans multiple entry types and adds a column only relevant to a subset (e.g., `extracted_text` only used for MEDIA entries), the adversarial review should flag unconditional SELECT as a bandwidth/performance concern. The current checklist covers index usage and WHERE clause optimization but not column-level selectivity for polymorphic result sets. <!-- Source: post-mortem, second-brain #162, 2026-02-19 -->
- **Local vs. post-push review find different issue classes.** PR #159 ran full local review (9 issues found pre-push) but CodeRabbit found 8 more post-push. Local review caught trigger gaps, query fields, JSDoc, sort logic. Post-push review caught test mock correctness, assertion shape completeness, type mismatches, and input validation. The adversarial review checklist is effective for production code but weak on test code quality (mock correctness, assertion completeness, fixture consistency). <!-- Source: post-mortem, second-brain #159, 2026-02-19 -->
- **New gap: CSS custom property validation.** Undefined `var()` references silently produce invalid styles. The adversarial checklist covers semantic elements and a11y but not CSS variable correctness. PR #164 shipped `var(--border)` and `var(--surface-hover)` which weren't defined. A Tier 0 grep for `var(--` cross-referenced against `--` definitions would catch this. <!-- Source: post-mortem, second-brain #164, 2026-02-20 -->
- **New gap: aria attribute pairing.** `aria-expanded` without `aria-controls` is incomplete. The checklist covers semantic elements (Tier 3) but not aria attribute completeness for collapsible/expandable patterns. <!-- Source: post-mortem, second-brain #164, 2026-02-20 -->
- **New gap: Popover/portal state cleanup on mode transitions.** When a component has multiple interactive modes (view, edit, date-picker-open), entering one mode should reset state for other modes. PR #186's DatePicker stayed logically "open" (showDatePicker=true) when user clicked Edit, causing it to reappear after editing ended. The adversarial review should verify: "When entering any interactive mode, are competing mode states reset?" <!-- Source: post-mortem, second-brain #186, 2026-02-20 -->
- **New gap: Popover repositioning on scroll/resize.** Portal-rendered popovers that compute position once on mount become disconnected from their trigger on scroll or window resize. The adversarial review should check: "Does this portal/popover recalculate position on scroll/resize, or at minimum close on scroll?" Both Copilot and CodeRabbit flagged this independently. <!-- Source: post-mortem, second-brain #186, 2026-02-20 -->
- **New gap: Silent error revert in optimistic UI.** Optimistic updates that revert on failure without any user-visible feedback create confusion — the user sees a change, then it silently disappears. The adversarial review should check: "After optimistic revert, is there user-facing error feedback (toast, inline error, etc.)?" <!-- Source: post-mortem, second-brain #186, 2026-02-20 -->
- **New gap: Defensive filtering for migration-gated type removal.** When removing a value from a type union AND gating the DB cleanup on a manual migration, code that queries the DB should defensively filter by supported types (e.g., `type IN (...)`) until the migration is confirmed run. Without this, legacy rows of the removed type can surface at runtime and crash downstream code that no longer handles them. Copilot caught two instances in search.ts. <!-- Source: post-mortem, second-brain #191, 2026-02-20 -->
- **New gap: Documentation count/state consistency on removal PRs.** When removing an entity from the system, docs that reference counts (e.g., "seven intents") or assert universal behavior (e.g., "AI responses persisted for all types") need updating. The adversarial review Tier 4 doc sync check should mechanically grep for count references and universal claims in docs touched by the diff. <!-- Source: post-mortem, second-brain #191, 2026-02-20 -->
- **Adversarial review "identified but not fixed" is worse than "not identified."** PR #198's adversarial review explicitly noted `buildTodoInlineButtons` duplication but chose to keep it with "keep-in-sync" comments. CodeRabbit flagged the same issue post-push. When the adversarial review identifies code duplication of identical functions, the default action should be extraction -- not documentation of the duplication. "We know about this and chose not to fix it" is a 0% catch rate for the finding, same as not seeing it at all. <!-- Source: post-mortem, second-brain #198, 2026-02-21 -->
- **"Covered but not actioned" is a repeating adversarial review failure mode (2nd occurrence).** PR #206's adversarial review explicitly identified "completed_at trigger fires on UPDATE only" as Finding #4 (LOW) but chose not to fix it. CodeRabbit flagged the same issue post-push. This confirms PR #198's pattern. When the adversarial review identifies an issue, the default should be to FIX it. "Low priority" or "non-blocking" is not a valid skip reason -- the 5-minute local fix always costs less than the 15-minute post-push round-trip (review + fix commit + re-review wait). <!-- Source: post-mortem, second-brain #206, 2026-02-22 -->
- **TOCTOU race on count-based limits in transactions is a blind spot.** PR #208's adversarial review ran the full DB checklist but missed that `SELECT COUNT(*) ... INSERT` inside a serializable transaction still allows concurrent transactions to read the same count before either commits. Advisory locks (`pg_advisory_xact_lock(hashtext($1))`) are needed to serialize the critical section. The Tier 2 DB checklist covers "check-then-insert in transaction" but not count-based limits specifically. This is a distinct pattern from dedup (which unique indexes solve) — count limits need serialization. <!-- Source: post-mortem, second-brain #208, 2026-02-22 -->
- **New gap: Telegram inline keyboard cleanup after callback handling.** PR #209 shipped callback handlers (`handleResearchCallback`) that edited message text after confirm/cancel/error but did not clear the inline keyboard (`reply_markup: { inline_keyboard: [] }`), allowing users to re-trigger callbacks after they were already processed. The adversarial checklist has no specific item for clearing interactive UI elements after handling. This is a Telegram-specific variant of "UI state cleanup after action completion." Consider adding to Tier 3 or to a new Telegram-specific section. <!-- Source: post-mortem, second-brain #209, 2026-02-23 -->
- **Adversarial review execution gap is systemic, not per-PR (5th consecutive occurrence).** PRs #199, #206, #209, #211, and #237 all had post-push findings that mapped to existing adversarial checklist items. PR #237 had 88% coverage potential but only 60% actual shift-left rate. The gap is not checklist completeness (items exist) but mechanical execution (items not applied). The adversarial review needs a structural intervention — either automated grep-based verification for high-frequency miss categories (XML escaping, whitespace guards, error fallback paths, delete-before-ready patterns) or a mandatory pass/fail evidence log that must reference grep output per item. Without this, the gap will persist regardless of checklist additions. <!-- Source: post-mortem, second-brain #237, 2026-02-25 -->
- **React state management is the #1 adversarial review blind spot for UI PRs (3+ occurrences).** PR #215 had 5 of 11 post-push findings in React-specific patterns absent from the checklist: render-phase setState (calling setState during render body instead of useEffect), instance-unique element IDs (hardcoded `id="..."` and `name="..."` collide across multi-instance components), duplicate React keys from data-derived values (`key={value}` where array items can repeat), and local submitted flags to bridge parent-child state refresh gaps. These patterns account for 36-45% of post-push findings on UI PRs and are not coverable by current Tier 1-3 items. Three candidate checklist additions: (1) grep for `set[A-Z]` calls outside useEffect/handler bodies in `.tsx`, (2) grep for hardcoded `id="` and `name="` in `.tsx`, (3) grep for `key={` where value is a raw data field (not index or computed unique). <!-- Source: post-mortem, second-brain #215, 2026-02-24 -->

## Process Compliance

- **Small changes are not exempt from the development flow.** PR #153 (6 LOC, 1 file) skipped steps 3 (lint + test), 4 (code review loop), and went straight to push. The CLAUDE.md development flow applies regardless of change size — the steps exist for consistency, not just for catching bugs in large PRs. The agent rationalized skipping as "minimal change" but the process doesn't have a size-based exemption. <!-- Source: post-mortem, second-brain #153, 2026-02-17 -->
- **Tooling/config PRs with mechanical auto-fixes are safe review-skip candidates.** folio PR #3 (3242 LOC, 29 files) added ESLint flat config and fixed 51 existing violations (37 auto-fixed import sorts, 3 unused vars, 1 `any` type, 2 template literals). Skipped steps 3 (Playwright), 4a (simplification), 4c (CodeRabbit), 4d (adversarial) -- 62.5% compliance rate. Result: 0% fix-up ratio, 0 post-push findings. The high LOC count is misleading -- bulk is config file + mechanical import reordering with no semantic change. Key distinguisher from feature PRs: tooling PRs are syntactic (behavior-preserving), not semantic. The skip assessment is "good" because no review step would have found issues in auto-fixed import sort order. <!-- Source: post-mortem, folio #3, 2026-02-23 -->
- **Pattern-replication changes are the safest skip candidates.** nanny-app PR #33 (4-line conditional wrapper, 1 file) skipped plan (step 1) and code review loop (steps 4a-4d) with 0 post-push findings. The change reused an established `!selectedNanny?.isNightNurse` guard already applied to 2 sibling sections in the same component. Distinguish "trivial pattern replication" (safe to skip) from "small behavioral change" (risky to skip) when justifying step skips. <!-- Source: post-mortem, nanny-app #33, 2026-02-20 -->
- **LOC threshold is imperfect for React state changes.** PR #189 (11 LOC production code) skipped steps 4a-4e under the 50 LOC threshold. CodeRabbit found a valid correctness issue post-push: stale `imageError` useState when the component instance is reused with different props (needs `useEffect` reset). A `useState` + conditional render change has more semantic surface than LOC suggests. Consider: when a diff introduces React state hooks, run at least CodeRabbit (step 4c) regardless of LOC count. <!-- Source: post-mortem, second-brain #189, 2026-02-20 -->
- **Docs-only PRs still need living docs update check.** PR #203 (924 additions, docs-only product spec) skipped all review steps (4a-4e) as "docs-only, under 50 LOC of code." CodeRabbit flagged that the new product spec didn't update the existing living docs (PRODUCT_SPEC.md, DECISIONS.md, QA.md). The adversarial review Tier 4 "Documentation sync" check would have caught this. For docs-only PRs that add new feature specs, at minimum run the Tier 4 doc sync check — the "docs-only" justification for skipping the full review loop is valid for lint/test/simplification, but not for documentation completeness checks. <!-- Source: post-mortem, second-brain #203, 2026-02-22 -->
- **Skipping review loop on 600+ LOC PRs produces bad outcomes.** PR #209 (632 LOC, 11 files) — the agent silently skipped steps 4a-4e without running them or asking, resulting in 0% shift-left rate and 67% fix-up ratio. Both substantive post-push findings (inline keyboard state persistence, raw error message exposure) mapped to existing adversarial checklist items. The silent skip saved ~20 min of local review but cost ~30 min of post-push fix-review cycles. **Root cause: agent non-compliance, not user request.** The CLAUDE.md rule is explicit: "After step 3 passes, automatically run the review loop. Do not ask for permission — just run it." For PRs >= 50 LOC, the review loop must auto-run without asking — agents must not silently skip it. <!-- Source: post-mortem, second-brain #209, 2026-02-23 -->

## Automation Opportunities

- **UTC suffix enforcement via lint rule.** A custom ESLint rule or grep check for `new Date("...:00")` without trailing `Z` would catch the most common timezone bug class automatically. Multiple PRs have hit this. **Decision:** Deferred CI-level lint rules. Instead, Tier 0 automated grep checks in the adversarial review checklist serve the same purpose without per-project setup cost. If patterns are still missed after Tier 0 is live, escalate to GitHub Actions workflows. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **Run Biome lint before push to catch a11y issues.** PR #143 shipped a `span` with `role="button"` that Biome's `noStaticElementInteractions` rule would have flagged. Running `npm run lint` as part of the pre-push workflow would have prevented at least 1 of 3 fix rounds. The lint output was either not generated or not reviewed before push. <!-- Source: post-mortem, second-brain #143, 2026-02-17 -->
- **Add Stylelint to local pre-PR checks for web projects.** Stylelint is the largest single source of CodeRabbit tool findings (~40% of all tool findings across 149 PRs). These are CSS issues caught post-push that could be caught locally. Add `npx stylelint "**/*.css"` to the pre-PR check sequence for projects with CSS (second-brain, nanny-app). When adding a new lint tool, always compose it into the main `npm run lint` script (`biome lint . && npm run lint:css`) — a standalone `lint:css` script that isn't wired into `lint` will be forgotten by developers using the standard workflow. <!-- Source: CodeRabbit analytics export, 2026-02-20; PR review, second-brain #228, 2026-02-25 -->
- **Pin devDependency versions consistently.** When a project mixes exact pinning (`"2.1.1"`) and caret ranges (`"^17.3.0"`) for dev tooling, bot reviewers flag the inconsistency. Pick one strategy (exact or caret) for all dev-tool deps and apply consistently. This is a trivial fix but generates a full review round when missed. <!-- Source: post-mortem, second-brain #228, 2026-02-25 -->

## Iteration Velocity

- **0% fix-up ratio with full local review loop.** PR #142 (650 LOC, 18 files) achieved zero post-push findings and zero fix commits by running the complete code review loop (simplifier → CodeRabbit → adversarial → CI) before push. CodeRabbit found 4 real issues locally; all were fixed before the PR was created. This is the target outcome — shift all quality catches to pre-push. **Confirmed again:** PR #157 (587 LOC, 19 files) also achieved 0% fix-up ratio with full local review loop — 5 issues caught and fixed pre-push (2 CodeRabbit + 3 adversarial). This is now a repeatable pattern for well-planned performance PRs. <!-- Source: post-mortem, second-brain #142, 2026-02-17; #157, 2026-02-19 -->
- **75% fix-up ratio on first tracked PR.** 3 of 4 commits were review fixes. All mechanical, all fast — but the ratio indicates the adversarial review is not catching enough pre-push. Target: <50% fix-up ratio. <!-- Source: post-mortem, second-brain #131, 2026-02-16 -->
- **0% fix-up ratio across PRs #135-#137.** Three consecutive clean PRs with no review-driven fix commits. However, PR #136 had a legitimate correctness finding that was left unaddressed (merged 1 min after CHANGES_REQUESTED). Zero fix-up ratio can mask ignored findings. <!-- Source: post-mortem, second-brain #135-137, 2026-02-17 -->
- **50% fix-up ratio on small focused PR.** PR #140 had 2 commits, 1 feature + 1 review fix. On small PRs (29 LOC), one review finding immediately produces a high ratio. Context matters more than the absolute number for small changesets. <!-- Source: post-mortem, second-brain #140, 2026-02-17 -->
- **75% fix-up ratio on pattern-adaptation PR despite local review.** PR #143 (242 LOC, 3 files) had 3 of 4 commits as review fixes, matching the worst performance in the series (PR #131). The PR adapted an inline-edit pattern from one component to another and claimed local adversarial review, but fixes were incomplete. Copy-and-adapt work is higher-risk for review quality than greenfield implementation. <!-- Source: post-mortem, second-brain #143, 2026-02-17 -->
- **0% fix-up ratio with unresolved review finding (false clean).** PR #145 (44 LOC, 3 files) had 1 commit and 0 fix commits, but merged with an outstanding CHANGES_REQUESTED. CodeRabbit found a spec-implementation mismatch (test asserts behavior contradicting the stated requirement). The 0% ratio is technically accurate but masks the unresolved finding. Same pattern as PR #136. When evaluating fix-up ratio, check whether CHANGES_REQUESTED reviews were addressed or ignored. <!-- Source: post-mortem, second-brain #145, 2026-02-17 -->
- **50% fix-up ratio with partial review addressing.** PR #148 (601 LOC, 23 files) had 2 commits, 1 feature + 1 fix. Addressed 2 of 4 actionable post-push findings (env leakage, TODO tracking), left 2 unaddressed (restoreAllMocks in afterEach, timeout test coverage). Merged with 2nd CHANGES_REQUESTED outstanding. Local review caught 6 issues pre-push (60% shift-left rate), but test isolation gaps still escaped both local and adversarial reviews. <!-- Source: post-mortem, second-brain #148, 2026-02-17 -->
- **80% fix-up ratio on large greenfield feature with full local review.** command-center PR #21 (1073 LOC, 9 files) ran the complete review loop (simplifier + CodeRabbit + adversarial + CI) locally, catching 13 issues pre-push (70% shift-left rate). Still, 3 post-push findings produced 4 fix commits (2 substantive + 2 marker). Fix-up ratio inflated by marker commits — substantive ratio was 67%. Key gap: date arithmetic correctness (UTC midnight normalization) slipped through despite Tier 3 date checks existing. <!-- Source: post-mortem, command-center #21, 2026-02-19 -->
- **80% fix-up ratio on large cross-cutting feature despite full local review and 100% step compliance.** PR #159 (887 LOC, 28 files) ran all 8 steps including local CodeRabbit (3 found, 2 fixed) and adversarial review (6 found, 5 fixed). Yet CodeRabbit still found 8 inline issues post-push across 2 rounds, producing 4 fix commits (1 feature + 4 fix). Marker commits inflate the ratio (substantive fixes = 2 commits), but the core issue is that local review found different issues than post-push review — test mock correctness, assertion strength, and type mismatches escaped local review entirely. Local pre-push review is necessary but not sufficient for test quality issues. <!-- Source: post-mortem, second-brain #159, 2026-02-19 -->
- **50% fix-up ratio with 86% shift-left rate on dual-concern PR.** nanny-app PR #28 (711 LOC, 12 files) bundled configurable pay periods + per-entry rate fix. Ran full local review loop for BOTH concerns (31 issues caught locally, 5 slipped to GitHub). Two CodeRabbit CHANGES_REQUESTED rounds before approval. The 86% shift-left rate is strong, but the dual-concern bundling makes rollback harder. For related-but-distinct fixes discovered during development, consider separate PRs unless the code paths are deeply interleaved. <!-- Source: post-mortem, nanny-app #28, 2026-02-20 -->
- **50% nominal fix-up ratio with marker commit inflation — substantive ratio 17%.** PR #164 (1019 LOC, 22 files) ran full review loop (100% compliance). 2 of 3 fix commits were infrastructure (marker file updates, gitignore fix), not code quality. Only 1 substantive fix commit addressed 4 review findings. Marker commits inflate fix-up ratio by 2x — consider excluding marker/infra commits from the ratio calculation. <!-- Source: post-mortem, second-brain #164, 2026-02-20 -->
- **Net-deletion refactor PRs have lowest review friction.** PR #185 (-126 net lines, 7 files) achieved 0 CHANGES_REQUESTED rounds and 33% fix-up ratio (1 doc fix out of 3 commits). The single post-push finding was documentation-only (JSDoc). Removal PRs with clear scope and graceful degradation for legacy state (redirect messages for old buttons) are the lowest-risk category. <!-- Source: post-mortem, second-brain #185, 2026-02-20 -->
- **75% fix-up ratio on greenfield UI component despite 100% step compliance and local adversarial review.** PR #186 (1565 LOC, 16 files) ran all 8 steps including adversarial review (2 found, 2 fixed). Yet Copilot found 4 issues and CodeRabbit found 8 across 3 CHANGES_REQUESTED rounds, producing 3 fix commits out of 4 total. The adversarial review caught fire-and-forget and a11y issues but missed: UTC date parsing inconsistency, popover repositioning on scroll/resize, silent error revert UX, and state cleanup on mode transitions. Custom UI components with portal rendering and positioning logic are high-risk for review findings. <!-- Source: post-mortem, second-brain #186, 2026-02-20 -->

- **50% fix-up ratio on large greenfield wizard component with 100% step compliance and 59% shift-left rate.** nanny-app PR #35 (1320 LOC, 6 files) ran all 8 steps, catching 10 of 17 total issues locally (2 CodeRabbit + 8 adversarial). Yet 7 post-push findings from CodeRabbit across 2 rounds produced 2 fix commits. Dominant category: JSDoc documentation (4/7 findings). The adversarial review caught "async error handling" but the fix was incomplete -- onComplete() still fired after catch. Fix-up ratio is moderate (50%) but could have been lower with a JSDoc grep check and more rigorous error-handling verification. <!-- Source: post-mortem, nanny-app #35, 2026-02-20 -->
- **75% fix-up ratio on large removal PR with 36% shift-left rate.** PR #191 (1783 LOC across 29 files, net -1369 lines) achieved only 36% shift-left rate (5 local catches vs. 9 post-push). Despite running nearly all steps (87.5% compliance, only Playwright skipped), 3 of 4 commits were fixes. The dominant post-push category was correctness (7 of 9 comments). Doc-related findings (count inconsistencies, spec-behavior mismatches) were the main local review blind spot — removal PRs need extra doc auditing because counts and universal claims silently become stale. Two new gap categories identified: migration-gated defensive filtering and doc count/state consistency. <!-- Source: post-mortem, second-brain #191, 2026-02-20 -->
- **0% fix-up ratio on LLM-to-regex replacement with 100% shift-left rate.** PR #192 (177 LOC, 4 files, net -73 lines) replaced an LLM call with deterministic regex. Single commit, zero fix commits. Local review caught 8 issues (4 internal + 4 adversarial), all fixed pre-push. CodeRabbit's sole post-push finding was a false positive (misread issue requirements). Deterministic replacements for LLM calls are the easiest PR category — testable edge cases, no nondeterminism, fully covered by parameterized tests. <!-- Source: post-mortem, second-brain #192, 2026-02-20 -->
- **50% fix-up ratio on large service extraction refactor with 80% shift-left rate.** PR #197 (1061 LOC, 12 files) consolidated 3 TODO creation paths into a unified TodoCreationService. Ran all 9 applicable steps (100% compliance). Local review caught 12 issues (4 internal + 8 adversarial), but 3 escaped to GitHub -- all were fallback-path semantic divergence issues (dueDate null vs undefined, missing explicit channel param, empty-string normalization). CodeRabbit found them in a single CHANGES_REQUESTED round, fixed in one fix-up commit. The 80% shift-left rate is strong for a 1000+ LOC PR but reveals a specific blind spot: when a new service has a legacy fallback path, local review doesn't systematically verify semantic parity between both paths for every parameter. New adversarial checklist item added. <!-- Source: post-mortem, second-brain #197, 2026-02-20 -->
- **50% fix-up ratio on small focused Telegram feature with 88% shift-left rate.** PR #198 (86 LOC, 4 files) re-rendered TODO lists after inline button actions. Ran 7 of 8 steps (87.5% compliance, justified Playwright skip). Local review caught 7 issues (1 internal + 3 CodeRabbit + 3 adversarial), but the adversarial review identified-but-did-not-fix a duplication issue. CodeRabbit flagged it post-push, producing 1 fix commit. For small PRs, 1 fix commit immediately produces 50% ratio. The specific gap: adversarial review choosing "keep-in-sync comments" over extraction for identical functions. <!-- Source: post-mortem, second-brain #198, 2026-02-21 -->
- **50% fix-up ratio on medium command consolidation PR with 77% shift-left rate.** PR #199 (878 LOC, 16 files) merged /brief and /summary into a single /brief command. Ran 7 of 8 steps (87.5% compliance, justified Playwright skip). Local review caught 10 issues (6 internal + 1 CodeRabbit + 3 adversarial). Yet 3 correctness findings escaped to post-push: residual process.env.USER_TIMEZONE read (directly contradicting the PR's stated objective to centralize timezone handling), test mock type mismatch (string vs Date), and whitespace normalization edge case. All 3 were covered by existing adversarial checklist items but not caught. Merged with outstanding CHANGES_REQUESTED (whitespace finding not addressed). <!-- Source: post-mortem, second-brain #199, 2026-02-21 -->
- **50% fix-up ratio on docs-only PR with skipped review loop.** PR #203 (924 additions, 4 docs files) skipped steps 3-4e as "docs-only." CodeRabbit found 3 issues in 1 CHANGES_REQUESTED round: missing living docs updates (documentation), heading level jump (markdown lint), missing fence languages (markdown lint). All addressed in 1 fix commit. For docs-only PRs, even a single round of bot review generates a fix commit when markdownlint and doc-sync checks are skipped locally. <!-- Source: post-mortem, second-brain #203, 2026-02-22 -->
- **50% fix-up ratio on docs-only PR updating cross-referencing specs (2nd occurrence).** PR #232 (83 LOC, 4 docs files) updated PRODUCT_SPEC, DECISIONS, QA, and PRODUCT_SPEC_RESEARCH_AGENT through PRs #219-#231. Skipped steps 3-4d as "docs-only, under 50 LOC of prose." CodeRabbit found 4 documentation issues across 2 CHANGES_REQUESTED rounds: stale section reference ("Section 3.3.1" nonexistent), incomplete removal annotations, conflicting sections (feed-header indicator marked removed in one place, active in another), and inconsistent cancellation docs. All fixed in 1 commit. This is the 2nd docs-only PR (after #203) where skipping the review loop produced a fix commit. For docs-only PRs that modify spec files with section numbering and cross-document references, run at least CodeRabbit local (step 4b) -- the "docs-only" exemption is valid for lint/test/simplification but not for reference consistency. <!-- Source: post-mortem, second-brain #232, 2026-02-25 -->
- **50% fix-up ratio on auth feature with 89% shift-left rate and 100% step compliance.** PR #205 (761 LOC, 20 files) added Supabase Auth magic link login. Ran all 8 steps. Local review caught 17 issues (2 internal + 4 CodeRabbit + 11 adversarial). Only 2 truly new findings post-push (JWT error logging specificity, API return value completeness); 2 other findings were already known/deferred from local review. All 4 post-push findings were CodeRabbit nitpicks (not blocking). Addressed 2 of 4 in fix commit, deferred 2 (pool close matches existing pattern, return boolean is design choice). This is the strongest shift-left rate for a 700+ LOC cross-cutting PR (DB + server + web). The auth domain (well-defined boundaries, standard patterns) may be inherently easier for local review than custom UI components. <!-- Source: post-mortem, second-brain #205, 2026-02-22 -->
- **25% fix-up ratio on schema-only PR with 88% shift-left rate.** PR #206 (358 LOC, 3 files) added research_tasks table with triggers, indexes, and shared TypeScript types. Ran 7 of 8 steps (87.5% compliance, justified Playwright skip). Local review caught 7 issues (4 internal + 3 adversarial), but 1 escaped: trigger event scope (completed_at fires UPDATE-only). The adversarial review identified this but classified it LOW and didn't fix it -- the second occurrence of "covered but not actioned" (after PR #198). Schema-only PRs are the lowest-friction category: single concern, no async patterns, no UI, deterministic review scope. The 25% fix-up ratio would have been 0% if the adversarial review had applied its own finding. <!-- Source: post-mortem, second-brain #206, 2026-02-22 -->
- **50% fix-up ratio on large service PR with 64% shift-left rate.** PR #208 (1373 LOC, 3 files) added ResearchService core (fire-and-forget pipeline, refine loop, query methods). Ran 7 of 8 steps (87.5% compliance, justified Playwright skip). Local review caught 9 issues (2 CodeRabbit local + 7 adversarial). CodeRabbit found 5 inline issues post-push across 2 CHANGES_REQUESTED rounds: advisory lock for TOCTOU race (correctness), defensive Date conversion (correctness), HTML-escape in notification (security), formattedContext escaping (security), pool shutdown (architecture). 3 of 5 fixed before merge, 2 not addressed. Fix-up commits: 1 of 2 total. Despite thorough planning (detailed plan with adversarial review), the advisory lock TOCTOU race and HTML-escape in notification methods were both in the adversarial checklist but missed during local review. The 64% shift-left rate is below the 80% target for PRs of this size. <!-- Source: post-mortem, second-brain #208, 2026-02-22 -->
- **67% fix-up ratio on mid-size Telegram integration PR with 0% shift-left rate (review loop silently skipped by agent).** PR #209 (632 LOC, 11 files) added research keyword detection, reply interceptor, and callback handling in TelegramAdapter. Steps 4a-4e (entire code review loop) were silently skipped by the agent — no user request to skip, no notification that it was skipped. CodeRabbit found 3 issues across 2 CHANGES_REQUESTED rounds: inline keyboard not cleared after callback (correctness), BadRequestError message exposed raw to user (security), escHtml inline instead of module scope (style). 2 fix commits out of 3 total. Both substantive findings map to existing adversarial checklist items (Tier 3 error message specificity, UI state cleanup). **Lesson: agents silently skipping mandatory steps is a worse failure mode than user-requested skips — at least explicit skips are recorded and reasoned about.** <!-- Source: post-mortem, second-brain #209, 2026-02-23 -->
- **40% fix-up ratio on 876 LOC route PR with 37.5% shift-left rate despite running most steps.** PR #211 (876 LOC, 9 files) added 8 REST endpoints with 48 tests. Ran 7 of 8 steps (CodeRabbit local skipped due to rate limit). CodeRabbit found 5 issues in 1 round — 3 of which map to existing adversarial checklist items (test env isolation, documentation sync, input validation). The adversarial review ran but caught 0 of 3 coverable findings. This is the 4th consecutive PR where the adversarial review has relevant checklist items but doesn't execute them mechanically. The dominant failure mode is no longer "checklist gap" but "checklist present, execution skipped." <!-- Source: post-mortem, second-brain #211, 2026-02-23 -->
- **67% fix-up ratio on 933 LOC greenfield feature with 60% shift-left rate despite running 7/8 steps.** PR #237 (929 additions, 23 files) added /sb command with pgvector semantic search, indexing script, and meta-conversation storage. Ran all steps except Playwright (justified backend-only skip). Local review caught 12 issues (4 code-simplifier + 5 CodeRabbit + 3 adversarial). Yet CodeRabbit found 8 more post-push across 2 CHANGES_REQUESTED rounds, producing 4 fix commits of 6 total. 7 of 8 post-push findings mapped to existing adversarial checklist items (XML escaping, error fallback, delete-before-ready, hash collision, whitespace guard, schema validation, transaction atomicity). The 88% adversarial catch potential vs. 60% actual shift-left rate confirms the persistent execution gap: the checklist covers the issues but the adversarial review doesn't mechanically apply them. This is the 5th PR in a row (after #199, #206, #209, #211) with covered-but-missed findings. <!-- Source: post-mortem, second-brain #237, 2026-02-25 -->

- **35.7% fix-up ratio on 17,990 LOC scaffolding PR with 86.8% shift-left rate.** folio PR #1 (72 files, 14 commits) scaffolded the entire MVP: Supabase schema, auth, accounts, balance updates, dashboard, activity feed, and Realtime. Ran steps 1-4d (skipped 3-Playwright and 4e-lint due to missing infrastructure). Local review caught 66 issues (13 internal, 38 CodeRabbit, 13 adversarial). Copilot found 10 post-push (2 inline + 8 suppressed low-confidence, all legitimate). Only 1 post-push fix commit vs. 4 local review fix commits. Dominant post-push category: security (RLS WITH CHECK) and correctness (partial index + ON CONFLICT, upsert timestamps, Realtime subscription completeness). The 86.8% shift-left rate is the highest for any PR over 1000 LOC -- scaffolding PRs benefit from sequential additive commits where each commit is self-contained. <!-- Source: post-mortem, folio #1, 2026-02-23 -->
- **Copilot "suppressed low-confidence" findings can be 100% legitimate.** folio PR #1 had 8 suppressed findings from Copilot (marked low-confidence), yet all 8 were valid bugs or security gaps that warranted fixes. Do not dismiss suppressed/low-confidence bot findings without reading them -- they are still worth reviewing even if the bot self-rated them as uncertain. <!-- Source: post-mortem, folio #1, 2026-02-23 -->

- **83% fix-up ratio on 1296 LOC UI PR with 100% step compliance — highest friction in project history.** PR #215 (10 files, 6 commits) ran all 8 steps including local CodeRabbit (10 found, 4 fixed) and adversarial review (2 found, 2 fixed). Yet CodeRabbit found 13 issues across 5 CHANGES_REQUESTED rounds post-push — the most review rounds for any PR. Shift-left rate was 31.6% (6 local / 19 total). Dominant category: React state management (render-phase setState, local submitted flags, instance-unique IDs, useEffect deps). 5 of 11 unique findings are React-specific patterns absent from the adversarial checklist. The internal review (4b) found 0 issues — indicating 4b's focus on "cross-file consistency" is insufficient for React component PRs. Local CodeRabbit found 10 issues but only fixed 4, suggesting a gap in local finding triage. <!-- Source: post-mortem, second-brain #215, 2026-02-24 -->

- **0% fix-up ratio on tooling PR with justified step skips.** folio PR #3 (3242 LOC, 29 files, 1 commit) added ESLint config and fixed 51 lint violations. Skipped CodeRabbit and adversarial review (62.5% compliance). Zero post-push findings, zero fix commits. High LOC is misleading -- 97% of changes are config + auto-fixed import ordering. Tooling PRs with mechanical changes are the lowest-risk category for review skips, even lower than "net-deletion refactor PRs" (second-brain #185). <!-- Source: post-mortem, folio #3, 2026-02-23 -->
- **0% fix-up ratio on small trivial bug fix with justified plan skip.** PR #225 (18 LOC, 3 files, 1 commit) removed an `onEntryCreated` callback causing unwanted auto-open. Skipped planning (step 1) and Playwright (step 3), ran all review steps. Single commit, zero post-push findings, CodeRabbit APPROVED without changes. Adversarial review caught 1 doc issue (BUGS.md) pre-push. This is the target outcome for trivial bug fixes: 100% shift-left rate, 0% fix-up ratio, 75% step compliance with justified skips. Contrasts with PR #153 (6 LOC) which skipped review steps entirely -- the difference is that #225 ran the review loop while only skipping inapplicable steps. <!-- Source: post-mortem, second-brain #225, 2026-02-24 -->
- **67% fix-up ratio on UI cleanup PR with 50% shift-left rate -- recently-added checklist items not executed.** PR #226 (1745 LOC nominal, ~645 production code + ~1100 mockup HTML, 8 files) removed research from ThreadPanel and collapsed related entries. Ran 7 of 8 steps (87.5% compliance, Playwright skipped for auth gate). Local review caught 5 issues (1 internal + 1 adversarial + 3 simplification). CodeRabbit found 5 substantive issues across 3 rounds: badge click propagation (correctness) and expanded state persistence (correctness) were both covered by checklist items added from PR #215's post-mortem hours earlier. This is the 5th consecutive PR where adversarial review has relevant checklist items but doesn't execute them mechanically. Merged with 3 outstanding CHANGES_REQUESTED reviews (3 findings unaddressed). Mockup HTML inflating LOC is a new consideration -- production code diff of ~645 LOC is at the threshold, not above it. <!-- Source: post-mortem, second-brain #226, 2026-02-25 -->
- **50% fix-up ratio on performance + tooling bundle PR with 33% shift-left rate.** PR #228 (1599 LOC, 8 files, 4 commits) deferred hidden panel fetches for faster dashboard load + set up stylelint. Ran all 8 steps (100% compliance). Local review caught 2 issues (CodeRabbit medium). CodeRabbit found 5 post-push findings across 2 rounds: 2 documentation (markdownlint), 3 style (lint composition, version pinning, CSS TODO). Zero correctness/security findings -- all post-push issues were tooling/convention. The bundled stylelint setup inflated LOC well beyond the performance optimization's true scope (CodeRabbit's pre-merge check warned about out-of-scope changes). The 33% shift-left rate is below target but the finding classes (markdownlint, npm script convention) are low-impact tooling items that don't affect runtime behavior. <!-- Source: post-mortem, second-brain #228, 2026-02-25 -->

## PR Sizing

- **Local review shift-left effectiveness degrades above ~600 LOC.** Across 15 tracked PRs with local review data, PRs under 600 LOC achieve 67-100% shift-left rates (catching that fraction of total issues before push), while PRs over 600 LOC drop to 14-59%. The two cleanest large PRs — #142 (654 LOC, 100% shift-left, 0% fix-up) and #157 (587 LOC, 100% shift-left, 0% fix-up) — both sat right at the threshold. Meanwhile, PRs at 887-1565 LOC consistently hit 2-3 CodeRabbit CHANGES_REQUESTED rounds and 50-80% fix-up ratios. **Counterexample:** PR #205 (761 LOC, 20 files) achieved 89% shift-left rate despite exceeding the 600 LOC threshold. Contributing factors: well-defined auth domain, standard patterns, full step compliance. **Recommendation:** Split features over 600 LOC into 2-3 focused PRs. A 1500 LOC feature split into three 500 LOC PRs would likely push each PR's shift-left rate above 80%, reducing total review rounds from ~6 to ~3. <!-- Source: cross-PR analysis of 15 PRs with local review data, 2026-02-20; counterexample: second-brain #205, 2026-02-22 -->

## Documentation Review Noise

- **Markdownlint findings inflate review rounds on docs PRs.** PR #141 had 2 review rounds, both on a README.md file: missing blank lines around headings/tables and missing fence language. These are mechanical lint issues, not code quality problems. When a PR includes docs, expect 1 extra CodeRabbit round for markdown lint. Mitigation: run markdownlint locally before push, or accept the noise as baseline. <!-- Source: post-mortem, second-brain #141, 2026-02-17 -->
- **Markdownlint is the dominant finding category on pure docs PRs.** PR #203 (869-line product spec) had 2 of 3 CodeRabbit findings related to markdown lint (heading level jump MD001, missing fence languages MD040, missing blank lines MD031). Running `npx markdownlint-cli2 docs/*.md` before push would eliminate these entirely for docs-heavy PRs. <!-- Source: post-mortem, second-brain #203, 2026-02-22 -->

## Review Discipline

- **Merging immediately after CHANGES_REQUESTED bypasses review purpose.** PR #136 was merged 1 minute after CodeRabbit flagged a race condition (stale closure in background refresh). The finding was legitimate and could cause stale data to flash on rapid tab switching. Even for bot reviews, acknowledge findings explicitly before merging: dismiss with a reason, or fix. **Repeat occurrences:** PR #145 merged 3 minutes after CodeRabbit flagged a test-spec behavioral mismatch. PR #148 merged ~2 minutes after CodeRabbit's 2nd review (CHANGES_REQUESTED) with 2 unaddressed findings (restoreAllMocks in afterEach, missing timeout test coverage). PR #199 merged with CHANGES_REQUESTED outstanding (whitespace normalization edge case in BriefHandler not addressed). PR #205 merged ~1 min after fix commit, before CodeRabbit could re-review (2 of 4 nitpick findings deferred without dismissal comment). This is now a 5-PR pattern. <!-- Source: post-mortem, second-brain #136, #145, #148, #199, #205, 2026-02-17-22 -->
- **Self-merge before any GitHub review eliminates the external feedback loop.** PR #157 (587 LOC, 19 files) was merged 3 minutes after creation — before CodeRabbit could submit a review. The local review loop caught 5 issues, but without GitHub-side review, the adversarial catch rate (1.0) is nominally perfect yet unverified. This is worse than merging after CHANGES_REQUESTED because there is zero external data. For PRs over 300 lines, waiting 10+ minutes for CodeRabbit provides the external baseline the self-improvement dashboard depends on. <!-- Source: post-mortem, second-brain #157, 2026-02-19 -->
- **CodeRabbit rate limits cause review gaps.** Three PRs submitted within minutes (#135, #136, #137) hit the hourly review limit. PR #137 received no code review at all before merge. Mitigation: space out PR submissions by 10+ minutes, or wait for rate limit expiry (~8 min) before merging. <!-- Source: post-mortem, second-brain #136-137, 2026-02-17 -->
- **CodeRabbit monorepo false positives are recurring noise.** CodeRabbit's sandbox runs `npm test` without building shared packages first, producing false test failures on every monorepo PR. This adds review rounds without value. Consider documenting as known limitation or configuring CodeRabbit to skip test execution. <!-- Source: post-mortem, second-brain #135, 2026-02-17 -->
- **CodeRabbit can misread issue requirements, producing false-positive CHANGES_REQUESTED.** PR #192: CodeRabbit claimed an "LLM fallback" was required by issue #171, when #171 explicitly called for *replacing* the LLM call with regex. The owner correctly dismissed with a detailed justification. When a bot finding contradicts the linked issue's stated requirements, verify the issue text before acting on the finding. <!-- Source: post-mortem, second-brain #192, 2026-02-20 -->
- **Two CodeRabbit rounds confirmed for 700+ LOC single-project PRs.** nanny-app PR #28 (711 LOC, 12 files) had exactly 2 CHANGES_REQUESTED rounds before approval. Round 1: 4 findings (memoization, test coverage, JSDoc). Round 2: 2 findings (stale-date deps, anchor validation). This extends the pattern beyond monorepo cross-cutting PRs to single-project feature PRs of similar size. **Confirmed again:** nanny-app PR #35 (1320 LOC, 6 files) had exactly 2 CHANGES_REQUESTED rounds. Round 1: 4 inline findings. Round 2: 3 inline findings. This is the 6th large PR confirming the 2-round norm. <!-- Source: post-mortem, nanny-app #28, 2026-02-20; nanny-app #35, 2026-02-20 -->
- **Two CodeRabbit rounds is the norm for large cross-cutting features.** PR #159 (887 LOC, 28 files spanning DB, server, web) had 2 CHANGES_REQUESTED rounds before a final COMMENTED approval. Round 1 found 5 issues (2 major, 3 trivial). Round 2 found 3 issues (1 critical, 2 minor). The two-round pattern is consistent with PR #131 and should be planned for on large PRs — budget 30+ minutes for CodeRabbit review cycles. **Confirmed again:** PR #164 (1019 LOC, 22 files) had exactly 2 CHANGES_REQUESTED rounds. Round 1: 5 comments (1 architecture, 2 correctness, 2 style). Round 2: 1 follow-up nit. This is now the 4th large PR with the 2-round pattern. <!-- Source: post-mortem, second-brain #159, 2026-02-19; #164, 2026-02-20 -->
- **Five CodeRabbit rounds when fix commits introduce new issues (cascading review).** PR #215 (1296 LOC, 10 files) set a new high with 5 CHANGES_REQUESTED rounds. Unlike the 2-round norm, rounds 2-5 each found NEW issues introduced or revealed by the previous round's fixes (render-phase setState emerged from flicker fix, biome-ignore needed after useEffect migration, cancel handler error state found after feedbackError was surfaced, test gaps found after cancel fix). This is a cascade pattern: each fix exposes the next layer of issues. Contributing factor: React state management changes have high interconnection — fixing one state flow affects adjacent flows. Budget additional review time when fix commits touch React state. <!-- Source: post-mortem, second-brain #215, 2026-02-24 -->

---
*Sources: post-mortem analysis across all projects*
